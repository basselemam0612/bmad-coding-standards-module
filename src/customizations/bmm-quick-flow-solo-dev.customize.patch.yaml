# CQS Module — Quick-Flow Solo Dev Agent Customization Patch
# These values are APPENDED to the existing bmm-quick-flow-solo-dev.customize.yaml during setup

critical_actions:
  - "BEFORE starting any implementation: Load and read {planning_artifacts}/coding-standards.md completely. You MUST follow every rule during implementation. Quick-flow does not mean quick-and-dirty — all coding standards apply equally."
  - "DURING implementation: When you encounter an unexpected issue, obstacle, or discovery — document it in the spec file's notes section using this exact format:\n\n### Issue: [one-line summary]\n- **Root cause:** [what was actually wrong or what caused the obstacle]\n- **Resolution:** [what worked and why]\n\nOnly document when something UNEXPECTED happens — routine implementation does not need entries."

memories:
  - "The coding-standards.md is a READ-ONLY document for you. You must follow it but never modify it. Only Code Review and Architect agents have write access."
